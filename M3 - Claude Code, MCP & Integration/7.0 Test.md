--- QUIZ ---

1. When configuring security controls for Claude Code execution in a sensitive codebase environment, an enterprise team wants to ensure that critical infrastructure directories remain protected even if a developer switches into a permissive bypass mode. According to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), what mechanism properly covers this security gap?

* A) Enforcing interactive prompt-before-everything mode via `CLAUDE.md`.
* B) Defining path-level deny rules at the project or enterprise level.
* C) Enabling `disable-model-invocation: true` in the project root configuration.
* D) Relying on subagent clean-context isolation to restrict host filesystem access.

2. During an AI-driven automated code review in a CI/CD pipeline, the reviewer flags two items: a missing null check on an explicit diff line, and an unproven claim that a downstream system will experience runtime deadlock under heavy thread concurrency. Based on the review principles in [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), how should the engineering team handle these findings?

* A) Automatically reject the pull request based on both findings because AI code reviews generate binding verdicts.
* B) Reject the null check finding unless accompanied by unit test assertions, but automatically apply the thread deadlock fix.
* C) Trust the null check finding verifiable directly from the diff, and treat the thread deadlock claim as a hypothesis requiring empirical testing.
* D) Pass the unproven runtime claim to a subagent to automatically modify system parameters while ignoring static diff checks.

3. A developer creates a `SKILL.md` workflow designed to run across Claude Code, the Messages API, and the Agent SDK. Which statement correctly describes how runtime execution and loading differ across these environments according to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd)?

* A) Claude Code uses filesystem discovery, Messages API uses beta headers with execution containers, and the Agent SDK relies on `settingSources`.
* B) Claude Code requires `settingSources`, Messages API uses filesystem discovery, and the Agent SDK uses beta headers.
* C) Messages API and Claude Code both automatically preload subagent skills, while the Agent SDK prohibits container execution.
* D) All three runtimes automatically discover `SKILL.md` files from local subagent parent sessions without configuration.

4. An agentic workflow in Claude Code invokes a subagent to perform an isolated code refactoring sub-task. The primary session has several active skills loaded in context. What happens to those skills when the subagent spawns according to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd)?

* A) The subagent automatically inherits all active skills from the primary session's memory space.
* B) The subagent inherits only skills that contain explicit shell tool execution commands.
* C) The subagent prompts the user interactively to authorize skill transfer before executing.
* D) The subagent starts in a clean context state and does not automatically preload skills from the parent session.

5. A developer attempts to place all project architectural guidelines, directory-specific styling rules, deterministic safety guardrails, and complex exploration logs into a single `CLAUDE.md` file. According to the durable context principles in [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), why is this architecture an anti-pattern?

* A) `CLAUDE.md` dilutes as its size increases, whereas scoped rules files, deterministic hooks, and subagents each solve distinct context concerns.
* B) `CLAUDE.md` cannot store text longer than 1,024 bytes without triggering automatic file truncation.
* C) Hooks cannot read guardrail definitions unless they are stored inside `.mcp.json` manifest files.
* D) Subagents require all context rules to be hardcoded into system environment variables rather than project files.

6. An integration engineer packages a custom plugin containing skills and hooks to distribute across an enterprise development team. Upon distribution, team members report that the plugin crashes on launch across multiple workstations. What packaging defect is most likely responsible for this issue according to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd)?

* A) The plugin manifest referenced relative paths instead of absolute home directory paths.
* B) The plugin relied on `PostToolUse` hooks rather than prompt-before-everything permission modes.
* C) The plugin referenced an absolute path specific to the author's local home directory instead of relative project paths.
* D) The plugin was committed to `.mcp.json` using HTTP transport protocol endpoints.

7. A developer configures an MCP tool server using `stdio` transport inside a repository's project-scoped `.mcp.json` file, expecting all remote team members to immediately share access to the running server instance. Why is this deployment configuration flawed according to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd)?

* A) `stdio` transport only operates when wrapped inside a `PostToolUse` audit logging hook.
* B) `stdio` transport is designed for local machine processes, whereas a shared team server requires HTTP transport paired with project or enterprise scope.
* C) Project-scoped `.mcp.json` files exclusively accept OAuth access tokens and reject local command bindings.
* D) Enterprise scope automatically converts all HTTP servers into local `stdio` subprocesses upon git commit.

8. An enterprise lead architect must design a Claude Code integration that fulfills strict regulatory compliance requirements for identity, credential storage, access auditing, and configuration locking. Which technical stack directly aligns with these four requirements according to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd)?

* A) API keys in `CLAUDE.md`, `stdio` transport, pre-commit hooks, and user settings.
* B) Bypass permission mode, `disable-model-invocation: true`, local scope `.mcp.json`, and Agent SDK `settingSources`.
* C) Hardcoded bearer tokens in `.mcp.json`, HTTP transport, subagent memory buffers, and project-level rules.
* D) OAuth for user identity, environment variables for service credentials, `PostToolUse` hooks for audit logging, and Enterprise Managed Settings for configuration lock.

--- ANSWER KEY ---

Question 1: B

Explanation: According to [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), permission modes alone do not guarantee path safety; path-level deny rules set at the project or enterprise level cover the gap when permissive bypass modes are active on developer workstations.

Question 2: C

Explanation: As stated in [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), developers should trust findings directly provable from the diff (e.g., missing null checks), while treating unproven runtime behavior claims as hypotheses requiring empirical verification before taking action.

Question 3: A

Explanation: [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd) specifies that skills load and sandbox differently across environments: Claude Code relies on local filesystem discovery, the Messages API uses beta headers and code execution containers, and the Agent SDK requires explicit `settingSources` configuration.

Question 4: D

Explanation: In every runtime environment covered in [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), subagents start in a clean context state and do not automatically inherit or preload skills from the parent session.

Question 5: A

Explanation: [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd) highlights that forcing all concerns into `CLAUDE.md` creates a bloated file that dilutes prompt effectiveness. Scoped rules, deterministic hooks, subagents, and `CLAUDE.md` each serve distinct context management roles.

Question 6: C

Explanation: A key takeaway from [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd) is that shareable components must use paths relative to the project root; embedding absolute local paths (such as the author's home directory) causes installations on other workstations to fail.

Question 7: B

Explanation: As explained in [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd), `stdio` is strictly for local host processes. Sharing an MCP server across a team requires using the HTTP transport protocol paired with project or enterprise configuration scopes.

Question 8: D

Explanation: [Claude Code, MCP & Integration](https://anthropic-partners.skilljar.com/path/claude-certified-developer-foundations/claude-code-mcp-integration/486744/scorm/173ln01ww7hgd) details that enterprise security requires OAuth for user identity, environment variables for service credentials, `PostToolUse` hooks for audit logging, and Enterprise Managed Settings for locking configurations.
